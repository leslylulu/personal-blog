---
interface Props {
  label: string;
  link: string;
  target?: string;
}
const { label, link, target } = Astro.props;
---

<a target={target ?? "_self"} href={link} class="text-white underline transition-opacity hover:opacity-90">
  {label}
</a>
